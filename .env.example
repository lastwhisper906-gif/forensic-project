# Anthropic API (필수)
ANTHROPIC_API_KEY=sk-ant-...

# SEC EDGAR 식별자 (필수) — 이메일 형식 권장
SEC_USER_AGENT=your-app/1.0 your@email.com

# API 보안 키 (Railway 배포 시 반드시 설정)
API_KEY=your-random-secret-key

# DB (미설정 시 SQLite forensic.db 사용)
# PostgreSQL 사용 시:
# DATABASE_URL=postgresql://user:password@host:5432/dbname
